{
  "name": "ai-teacher-wrapper",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd ai-tutor && npm start",
    "build": "cd ai-tutor && npm run build",
    "test": "cd ai-tutor && npm test",
    "eject": "cd ai-tutor && npm run eject"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "mongoose": "^8.13.2"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^14.6.1",
    "identity-obj-proxy": "^3.0.0",
    "web-vitals": "^4.2.4"
  }
}
